# Run the app locally

Open `Rcheology.Rproj` in RStudio, then either open `app/app.R` and click
**Run App**, or run this from the Console:

```r
shiny::runApp("app")
```

The app uses `shiny`, `bslib`, `DT`, and `dplyr`.
